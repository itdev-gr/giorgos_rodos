<div class="th-hero-wrapper hero-1 hero-fullscreen" id="hero">
  <div class="hero-inner">
    <div class="th-hero-bg">
      <video autoplay muted loop playsinline style="width: 100%; height: 100%; object-fit: cover; position: absolute; top: 0; left: 0;">
        <source src="/assets/img/hero/hero-video.webm" type="video/webm" />
      </video>
      <div class="hero-overlay"></div>
    </div>
    <div class="container">
      <div class="hero-style1">
        <div class="hero-content">
          <span class="hero-accent">Private Yacht Charters</span>
          <h1 class="hero-heading">Discover the Greek Islands</h1>
          <p class="hero-subtitle">Bespoke voyages crafted around your desires</p>
        </div>
        <div class="scroll-indicator">
          <span class="scroll-text">Scroll</span>
          <div class="scroll-arrow">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M12 5v14M5 12l7 7 7-7" />
            </svg>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .hero-fullscreen {
    height: 100vh;
    min-height: 600px;
  }

  .hero-fullscreen .hero-inner {
    height: 100%;
  }

  .hero-fullscreen .th-hero-bg {
    height: 100%;
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      180deg,
      rgba(0, 0, 0, 0.35) 0%,
      rgba(0, 0, 0, 0.15) 40%,
      rgba(0, 0, 0, 0.3) 100%
    );
    z-index: 1;
  }

  @media (max-width: 991px) {
    .hero-overlay {
      background: none;
    }
  }

  .hero-fullscreen .container {
    height: 100%;
  }

  .hero-fullscreen .hero-style1 {
    padding: 0 0 0 3%;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
    position: relative;
    z-index: 2;
  }

  .hero-content {
    text-align: left;
    color: #fff;
    max-width: 700px;
  }

  .hero-accent {
    display: block;
    font-family: 'Cormorant Garamond', serif;
    font-style: italic;
    font-size: 1.25rem;
    font-weight: 400;
    letter-spacing: 3px;
    text-transform: uppercase;
    margin-bottom: 1rem;
    opacity: 0.9;
  }

  .hero-heading {
    font-family: 'Manrope', sans-serif;
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 1.25rem;
    color: #fff;
    letter-spacing: -0.5px;
  }

  .hero-subtitle {
    font-family: 'Inter', sans-serif;
    font-size: clamp(1rem, 2vw, 1.25rem);
    font-weight: 300;
    margin-bottom: 2.5rem;
    opacity: 0.85;
    letter-spacing: 0.5px;
    color: #fff;
  }

  .hero-cta {
    display: inline-block;
    padding: 16px 40px;
    font-family: 'Inter', sans-serif;
    font-size: 13px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 2px;
    color: #fff;
    background: transparent;
    border: 1.5px solid rgba(255, 255, 255, 0.7);
    border-radius: 4px;
    text-decoration: none;
    transition: all 0.4s ease;
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
  }

  .hero-cta:hover {
    background: #fff;
    color: #113D48;
    border-color: #fff;
  }

  /* Scroll indicator */
  .scroll-indicator {
    position: absolute;
    bottom: 40px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    color: rgba(255, 255, 255, 0.7);
  }

  .scroll-text {
    font-family: 'Inter', sans-serif;
    font-size: 11px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 2px;
  }

  .scroll-arrow {
    animation: bounce 2s ease infinite;
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(8px);
    }
    60% {
      transform: translateY(4px);
    }
  }

  @media (max-width: 767px) {
    .hero-accent {
      font-size: 1rem;
      letter-spacing: 2px;
    }

    .hero-cta {
      padding: 14px 32px;
    }

    .scroll-indicator {
      bottom: 24px;
    }
  }
</style>
